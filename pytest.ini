[pytest]
testpaths = tests
python_files = test_*.py

# ``vendor/`` holds read-only source snapshots used for provenance and rule
# comparison. Their own test suites have separate dependencies and must not be
# collected as if they tested this simulator.
